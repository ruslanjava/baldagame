<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="LetterView" parent="@android:style/TextAppearance.Widget.TextView">
        <item name="layout_widthPercent">20%</item>
        <item name="layout_heightPercent">20%</item>
    </style>

    <style name="KeyboardLetterView" parent="@android:style/TextAppearance.Widget.TextView">
        <item name="android:layout_width">0dp</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_margin">1dp</item>
        <item name="android:textSize">16sp</item>
        <item name="android:gravity">center</item>
        <item name="android:padding">1dp</item>
        <item name="android:background">@drawable/normal_cell_background</item>
    </style>

    <style name="KeyboardDialogAnimation">
        <item name="android:windowEnterAnimation">@anim/keyboard_dialog_show</item>
        <item name="android:windowExitAnimation">@anim/keyboard_dialog_dismiss</item>
    </style>

</resources>
